'use strict';

describe('scrumPoker.developer module', function() {

  beforeEach(module('scrumPoker.developer'));

  describe('developer controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('DeveloperCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});